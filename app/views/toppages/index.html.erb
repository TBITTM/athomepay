<% if logged_in? %>
  <p><%= @kidsname %>ちゃんの現在残高</p>
<% else %>
<div class="bg-secondary p-3 p-sm-5 my-4 rounded">
  <div class="text-center text-white">
    <h1>アプリ説明画面</h1>
    <%= link_to '初めましての方はこちら（新規登録）', signup_path, class: 'btn btn-lg btn-primary' %>
  </div>
</div>
<% end %>